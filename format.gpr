library project format is

   for Source_Dirs use ("src/format");
   for Object_Dir use "obj";
   for Library_Name use "format";
--   for Library_Dir use "bin";
   for Library_Kind use "static";

   package Compiler is
      for Default_Switches ("ada") use ("-O3", "-gnat12");
   end Compiler;
   
end format;

